SuperWallet Render deployment

Build command: npm install
Start command: npm start
Environment variables required on Render: MONGODB_URI, JWT_SECRET, MASTER_USERNAME, MASTER_EMAIL, MASTER_PASSWORD, NODE_ENV=production, FRONTEND_ORIGIN, BIGBANG_API_KEY (if using BigBang demo catalogue).
Do not upload .env or node_modules.
